import React, { useRef } from "react";
import type { ActionType, ProColumns } from "@ant-design/pro-components";

// 编辑页面 -> 左侧选择栏，中间画布，右侧数据编辑栏
const Editor: React.FC = () => {
  const actionRef = useRef<ActionType>();
  return <div>123</div>;
};

export default Editor;
